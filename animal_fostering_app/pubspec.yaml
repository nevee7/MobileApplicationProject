name: animal_fostering_app
description: A complete animal fostering and adoption mobile app.

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

version: 1.0.0+1

dependencies:
  flutter:
    sdk: flutter

  # Core UI
  cupertino_icons: ^1.0.2
  google_fonts: ^6.3.2

  # HTTP & API
  http: ^1.5.0

  # Storage
  shared_preferences: ^2.2.2

  # Remove these temporarily to reduce complexity:
  # google_maps_flutter: ^2.14.0
  # geolocator: ^11.1.0
  # image_picker: ^1.2.0
  # camera: ^0.10.6
  # flutter_local_notifications: ^16.3.3
  # web_socket_channel: ^2.4.0
  # lottie: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  # assets:
  #   - assets/animations/